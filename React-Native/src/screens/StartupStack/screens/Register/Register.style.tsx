import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: -70,
  },

  buttonContainer: {
    marginTop: 30,
  },
  spaceInputs: {
    marginTop: 20,
    marginBottom: 10,
  },
});

export {styles};
