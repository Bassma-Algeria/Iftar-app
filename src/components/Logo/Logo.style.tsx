import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'baseline',
  },

  moon: {
    position: 'absolute',
    top: -3,
    left: 4,
    width: 20,
    height: 20,
  },
});

export {styles};
